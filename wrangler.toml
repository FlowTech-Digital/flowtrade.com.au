# FlowTrade CloudFlare Pages Configuration
# Last rebuild: 2025-12-13T00:45:00+11:00 (fix: clear .open-next for chunk coherence)
name = "flowtrade"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".open-next/cloudflare"

# CRITICAL: Clear ALL build artifacts to prevent stale chunk references
[build]
command = "rm -rf .next .open-next node_modules/.cache && npx @opennextjs/cloudflare build"

[vars]
NEXT_PUBLIC_APP_NAME = "FlowTrade"
NEXT_PUBLIC_APP_URL = "https://flowtrade.com.au"
